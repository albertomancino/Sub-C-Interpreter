#include <stdio.h>

int fun(int x){
  int y = 10;
  //y = y+x+++x++;

  return x--;
}

int main(){

  int tt = 10;
  int vec[3] = {1,2,3};
  if ('d'||'s'){
    printf("PRIMO IF\n" );
  }
  int x = 0;
  printf("x: %d\n", x);
  if(1 || x++){
    printf("INSIDE IF\n");
  };
  printf("x: %d\n", x);
  if (x++){
  }
  if (x++){
  }

}
